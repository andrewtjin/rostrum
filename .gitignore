# Dependencies & build output
node_modules/
dist/
*.tsbuildinfo
coverage/

# Logs
*.log
npm-debug.log*

# OS / editor cruft
.DS_Store
Thumbs.db

# Office add-in sideload / dev-cert scratch
.office-addin-debugging/

# Local agent + knowledge-graph tooling scratch (generated; never ship)
.claude/
graphify-out/
.graphify_python

# Local-only synthetic Docs-JSON scale fixtures + measurements derived from
# personal debate docs (tools/gdocs-synth-fixture.mjs writes them; the
# gdocsRealDocs suite is skip-when-absent, so CI never needs them)
samples/
